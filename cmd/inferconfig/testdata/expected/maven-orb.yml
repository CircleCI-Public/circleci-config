# This config was automatically generated from your source code
# Stacks detected: deps:java:sample_app2
version: 2.1
jobs:
  test-java:
    docker:
      - image: cimg/openjdk:17.0
    working_directory: ~/project/sample_app2
    steps:
      - checkout:
          path: ~/project
      - restore_cache:
          key: maven-{{ checksum "pom.xml" }}
      - run:
          command: mvn verify
      - store_test_results:
          path: target/surefire-reports
      - save_cache:
          key: maven-{{ checksum "pom.xml" }}
          paths:
            - ~/.m2/repository
  deploy:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: cimg/base:stable
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: deploy
          command: '#e.g. ./deploy.sh'
workflows:
  build-and-test:
    jobs:
      - test-java
    # - deploy:
    #     requires:
    #       - test-java
